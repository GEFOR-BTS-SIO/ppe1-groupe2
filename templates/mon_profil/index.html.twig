{% extends 'base.html.twig' %}

{% block title %}Hello MonProfilController!
{% endblock %}

{% block body %}
	<div class="flex items-center justify-center h-screen">
		<div class="flex flex-col md:flex-row items-center md:w-3/4 lg:w-1/2 bg-white shadow-lg rounded-lg p-8">
			<div class="md:w-1/3 mb-4 md:mb-0">
				<img src="{{ asset('uploads/photos/' ~ app.user.photo) }}" class="w-20 h-20 object-cover rounded-full border-2 border-indigo-500 " alt="Profile Picture">
			</div>
			<div class="md:w-2/3">
				<h2 class="text-2xl font-bold mb-4">Prenom:
					{{app.user.name}}</h2>
				<h2 class="text-2xl font-bold mb-4">Nom:
					{{app.user.firstname}}</h2>
				<p class="text-gray-700">Fromation:
					{{app.user.formation}}</p>
				<p class="text-gray-700">Email:
					{{app.user.formation}}</p>
				<div class="flex justify-center mt-4">
					<a href="{{ path('app_eleve_crud_edit', {'id': app.user.id}) }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800 hover:underline">Edit</a>
					<a href="{{ path('app_eleve_crud_index') }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800 hover:underline">Retour à la liste</a>

				</div>


			</div>
		</div>
	</div>
{% endblock %}
